# Future plans and modifications:

1. use a binary format instead of txt files for storage (.WAL)
2. manually implement self-balancing tree for the index (Red-Black or AVL)
3. implement SSTables

the final goal is to have a fully functional LSM-tree database engine.
